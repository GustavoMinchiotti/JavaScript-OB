// Notación

//? ; -> Delimitar el final de una línea
const b = 4;
b + 4;

//? . -> Se utiliza en los objetos para acceder a los atributos

//* taza.anchura  --> en este ejemplo quiso decir que cada objeto puede tener varios atributos y con el punto accedo a alguno de ellos.
//? ejemplo: objeto taza tiene --> altura, anchura, peso y color.

//? [] -> listas, arreglos o arrays
const ar = [1, 2, 3, 4]; //* así lo defino
console.log(ar[2]); //* asi busco un valor dentro

//? () -> Funciones
function suma(a, b) {   
  // Se escribe la función
}

//? {} -> Llaves para objetos, funciones y estructuras de control
const taza = {
  anchura: 5,
  altura: 10,
};

if (true) {
  // todo lo que haya entre llaves

  const constante2 = "hola";
}
